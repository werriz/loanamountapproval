spring:
  banner: 'banner.txt'
  output:
    ansi:
      enabled: ALWAYS

loan:
  approval:
    external:
      host: 'localhost:8081'
      managers:
        uri: '/managers'
      customers:
        uri: '/customers'